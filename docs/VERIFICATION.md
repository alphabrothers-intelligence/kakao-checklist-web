# 2026-09-15 브랜드 및 저장 화면 개선

- 메인 시안 문구·배치 반영. 메인·결과 캐릭터는 제공 PNG와 동일 그림의 로컬 SVG 원본 사용.
- Kakao Big Sans Regular/Bold 전면 적용. UI 키 컬러 #0C090A / #FAD524, 흰 배경 및 잉크 투명도 사용. 원본 캐릭터·아이콘 색상 유지.
- 버튼·선택지에 완전한 캡슐 라운드 적용. PC 다음 버튼을 선택지 한 칸 너비로 조정.
- 01 요약 카드 안쪽 흰 카드·구분선·포인트 추가, 업종 TOP1 노란 밑줄 복원.
- 저장 창은 모바일 이미지 우선, PC PDF 우선. 두 형식 모두 선택 가능.
- 별도 렌더링 화면에서 PNG는 390px 모바일 배치, PDF는 1080px PC 배치로 3배 해상도 저장.
- 테스트 608개·빌드 통과. PC·모바일 브라우저 회귀 검사, 기기별 저장 버튼 우선순위, 파일 다운로드 통과.
- 실제 PNG 모바일 배치와 PDF PC 배치 렌더링 육안 확인 완료. 실기기 검증은 미실시.

---

# 2026-09-15 결과 화면 디자인 개선

- 질문 단계 STEP 1~4, 선택 안내 문구 변경. 고객 행동 유형은 위, 설명은 아래에 표시.
- 결과 상단 배지·캐릭터 SVG 추가, 오른쪽 슬라이드 진입 및 동작 줄이기 대응.
- 응답 태그를 01 나의 광고 타입 카드로 교체. 기존 결과 카드는 02~04로 변경.
- 결과 카드 라운드, 원형 광고 순위, 제목·본문·버튼 문구 반영.
- 608개 테스트 및 빌드 통과. PC·모바일 대표 흐름과 PNG·PDF 다운로드 검증 통과.
- PC·모바일 결과 스크린샷에서 SVG, 요약 카드, 라운드, 추천 이유 표시 확인.

---

# 2026-09-15 로직·문구 변경 검증

- 기준: 사용자 제공 2026년 9월 15일 AM 1:00 문서와 질문 순서 이미지.
- `npm run check`: 608개 테스트 및 TypeScript·Vite 빌드 통과.
- 600개 응답 조합을 별도 보관한 원문 표와 대조: TOP3 광고명·이유, 경로별 운영 문구, 추천 광고 2개·이유, 선택 요약 일치.
- 질문 4개와 선택지·설명·순서 확인. 구버전 답변 초기화 검증.
- `node scripts/verify-browser.mjs http://127.0.0.1:5180`: 대표 PC·모바일 흐름, 뒤로/앞으로 이동, 새로고침 복구, 미완성 결과 접근, 초기화, 가이드 모달, PNG·PDF 저장 통과.
- 390px·320px 모바일 에뮬레이션 레이아웃 확인. 실기기 검증은 미실시.
- PC 질문·모바일 결과 스크린샷 육안 확인. 산출물: `artifacts/browser/`.
- 전체 600개 실제 브라우저 선택 흐름은 이번에 재실행하지 않음. 전체 매핑은 단위 테스트로 대조.
- 내장 브라우저 연결 오류로 기존 Playwright 검증 스크립트 사용.

---

## 이전 버전 검증 기록

# 개발 검증 기록

검증일: 2026-09-09

## 완료

- `npm run check`: 테스트 708개 통과. 기존 7개와 원문 대조 700개, 질문·선택지 원문 검사 1개 포함.
- TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- `node scripts/verify-browser.mjs http://127.0.0.1:5174`: 프로덕션 빌드 대상 Chrome 검증 통과.
- 데스크톱 1440×1000 / 모바일 에뮬레이션 390×844 / 좁은 화면 320px 검증.
- 질문 단일 선택, 이전/다음, 브라우저 뒤로/앞으로, 새로고침 후 선택 복구, 결과, 다시 진단 정상.
- 모바일 스크롤 후 마지막 선택지가 고정 CTA 위로 완전히 노출됨.
- 결과 버튼 3개는 같은 너비와 같은 행에 배치됨.
- PNG/PDF 실제 다운로드 성공. PNG 3240×7392px. PDF 내부 무손실 이미지와 PNG의 RGB 픽셀 일치.
- PDF를 Poppler로 렌더링하고 결과 레이아웃, 한글 및 이모지를 확인함.
- 모바일의 양쪽 문안 결과도 PNG 저장 성공.
- `node scripts/verify-accessibility.mjs`: 키보드 방향키 선택, 단일 포커스 표시, 단계 이동 제목 포커스, 작은 화면 가로 넘침 없음, 모션 줄이기 동작 확인.
- Vercel 설정 파일 작성 완료. 개발 서버 5173 / 빌드 미리보기 5174.

## 아직 수행하지 않은 것

- 실제 iOS Safari·Android 기기·카카오톡 인앱 브라우저 검증. Chrome 모바일 에뮬레이션만 검증함.
- ~~실무자 협의 후 메뉴얼·스타터킷 실제 URL 연결.~~ 2026-09-16 완료. 가이드 20개와 스타터 키트 링크를 연결했다. 가이드 20개는 공개 주소라 실제 브라우저로 페이지 제목까지 대조했다. 스타터 키트는 `app.notion.com` 워크스페이스 주소지만 비로그인 상태에서도 정상 열람되는 것을 사용자가 확인했다.

기존 HTML 시안은 `preview/`에 보존. 현재 개발 기준은 `src/`와 `public/`입니다.

## 사용자 제공 표와의 재대조 (2026-09-09)

기존 700개 조합 검사는 결과의 누락·형태를 확인하는 검사였으며 모든 문구의 원문 일치를 보장하지 않았다. 이번에는 앱 데이터와 별도로 원문을 옮긴 `tests/fixtures/approved.mjs`를 기준으로 검사한다.

| 대상 | 대조 범위 |
|---|---|
| 질문 | 4개 질문, 7·5·5·4개 선택지 및 보조 설명 |
| 결과 ① | 7개 업종 × TOP3: 광고명, 순서, 추천 이유 21개 |
| 결과 ② | 7종 문안: 병목명 및 상황 해결·구조 개선·주의 사항 21개 문장 |
| 결과 ③ | 5개 전환 경로 × 4개 목표: 추천 2개와 순서, 총 20개 매핑 |
| 전체 조합 | 7 × 5 × 5 × 4 = 700개 |

수정한 원문 차이: Q2와 Q3의 축약 질문을 원문으로 복원하고, 생활 서비스의 예시를 ‘세탁소, 수리점, 청소, 자동차 관련 서비스’로 복원했다. 광고명·추천 순서·이유·운영 안내는 원문과 일치하여 변경하지 않았다. 기타 업종은 이전 확정대로 제외한다.

### 기능 통과와 구분해야 하는 분기 상태

- 오프라인 방문형의 유입·측정 부족: 오프라인 매장 문안만 표시.
- 온라인 구매형의 유입·측정 부족: 온라인 사이트 문안만 표시.
- 관심·전환·재구매 부족: 경로에 관계없이 각 문제의 공통 문안 표시. 상담·예약에 관한 전환 부족 안내도 원문대로 포함.
- 예약 방문형·상담 전환형·플랫폼 이동형의 유입·측정 부족: 온라인·오프라인 문안을 함께 표시하는 현재 임시 동작. 해당 168개 조합은 단일 분기 최종 확정으로 간주하지 않는다.
- 결과 ③의 상담 전환형을 포함한 20개 추천 매핑은 위 임시 분기와 별개로 모두 정의되어 있다.

전체 브라우저 검사 재현: `node scripts/verify-all-combinations.mjs http://127.0.0.1:5174`. 프로덕션 빌드를 먼저 준비하고 해당 주소에 미리보기 서버를 실행한다. 데스크톱 Chrome에서 실제 라디오 선택·다음·결과·다시 진단하기를 반복하며 화면의 원문 일치를 검사한다. 이 검사는 모바일 실기기 검사를 대체하지 않는다.

실행 결과: **700/700 통과**, 브라우저 런타임 오류 0개. 미선택 시 다음 버튼 비활성화, 단계별 선택지 수, 단일 선택, 결과의 정확한 문구·순서·선택 요약, 다시 진단 후 초기화를 전 조합에서 확인했다. 결과 로그: `artifacts/qa/all-combinations.json`. 별도 `verify-browser.mjs` 회귀 검사도 통과했다(모바일 에뮬레이션, 답변 복구, PNG/PDF 다운로드, 가이드 모달 포함).

## GitHub 및 Vercel 연결 (2026-09-09)

- GitHub 저장소: https://github.com/alphabrothers-intelligence/kakao-checklist-web (`main`).
- Vercel 프로젝트: `intelligence2/kakao-checklist-web`, GitHub 저장소 연결 완료.
- 배포 주소: https://kakao-checklist-web.vercel.app
- Vercel 배포 상태 READY 확인. `.vercelignore`에서 앱의 `src/data`가 제외되던 문제를 수정.
- 분기 정책과 가이드 목적지는 실무자 협의 후 변경하며 현재 동작을 유지.
- 배포 주소 대상 Chrome 검사 통과: 진단·답변 복구·데스크톱/모바일 에뮬레이션·PNG/PDF 다운로드·다시 진단·가이드 모달.

## 결과 화면 및 수정 문안 반영 (2026-09-09)

- 최신 사용자 제공 표 기준: TOP3 추천 이유 14개, 운영 안내 문장 13개 교체. 상품·순서·분기 규칙 유지.
- 원문의 `받을 수있어요`, `노출을 써`, `방문 전환율이 높일 수 있어요` 등 표기를 임의 교정하지 않고 보존.
- 선택 요약 4개 유지·결과 화면의 답변 수정 버튼 제거.
- 01~03 섹션 제목을 번호와 동일한 15px·700 굵기로 통일.
- ‘발견된 항목’을 굵게 확대하고 문제명은 다음 줄에 배치. 온라인·오프라인 문안도 같은 구조.
- `npm run check`: 708개 테스트 및 빌드 통과. 700개 조합의 기대 문구 대조 포함.
- `verify-browser.mjs`: PC·모바일의 버튼 제거·선택 요약·제목 크기·굵기·세로 배치 추가 검사 통과. PNG/PDF 저장 및 기존 주요 흐름 통과.
- 데스크톱·모바일 스크린샷과 PDF 렌더링을 확인. 이번 변경에서 700개 브라우저 흐름 전체를 재실행하지는 않았음.
- README·결과 로직 문서·IA 이미지를 현재 기능에 맞게 수정.

## 후속 문구 교정 (2026-09-09)

- 사용자 요청에 따라 ‘받을 수있어요’ → ‘받을 수 있어요’, ‘방문 전환율이 높일 수 있어요’ → ‘방문 전환율을 높일 수 있어요’로 수정. 앞선 원문 보존 기록 중 해당 두 표현을 대체함.
- 앱 데이터·독립 기대값·전체 결과 로직 문서에 동일하게 반영.

## 운영 안내 카드의 정보 위계 조정 (2026-09-09)

- 대상 라벨·발견된 항목: PC 18px / 모바일 17px·일반 굵기·진한 색상. 문제명은 28px / 26px 볼드.
- 온라인·오프라인 라벨은 회색 배경으로 구분하고 카드 내부 노란 밑줄 제거.
- 이모지는 문제명 옆 1개로 제한. 소제목 17px / 16px, 본문 16px로 가독성 유지.
- 문구·추천·분기 조건 변경 없음. 테스트 708개·빌드 및 PC/모바일·PNG/PDF 브라우저 회귀 검사 통과.
